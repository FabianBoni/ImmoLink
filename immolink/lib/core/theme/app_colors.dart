import 'package:flutter/material.dart';

class AppColors {
  // Brand & Color Palette
  static const Color primaryBackground = Color(0xFFFFFFFF);
  static const Color surfaceCards = Color(0xFFF5F6FA);
  static const Color dividerSeparator = Color(0xFFE5E5E5);
  static const Color primaryAccent = Color(0xFF3366FF);
  
  // Text Colors
  static const Color textPrimary = Color(0xFF222222);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textPlaceholder = Color(0xFFAAAAA);
  
  // Status Colors
  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFF9500);
  static const Color error = Color(0xFFFF3B30);
  
  // Additional UI Colors
  static const Color accentLight = Color(0xFFE5F0FF); // For active tab backgrounds
  static const Color successLight = Color(0xFFE8F5E8); // Light success background
  static const Color warningLight = Color(0xFFFFF4E6); // Light warning background
  static const Color shadowColor = Color(0x0C000000); // rgba(0,0,0,0.05) for shadows
  static const Color overlayWhite = Color(0x80FFFFFF); // Semi-transparent white for overlays
}